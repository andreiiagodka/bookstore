.col-sm-3
  %h3.general-subtitle
    = t('checkout.delivery.title')
    %span.general-edit
      - unless params[:user_id]
        = link_to wizard_path(:delivery) do
          = t('checkout.confirm.edit')
  %p.general-address
    = delivery.name
