= f.hidden_field :cast, value: cast
.form-group{class: (@page_presenter.set_error_class(@user, :first_name))}
  = f.label :first_name, t('address.first_name'), class: 'control-label input-label'
  = f.text_field :first_name, class: 'form-control', placeholder: t('address.first_name'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :first_name)
.form-group{class: (@page_presenter.set_error_class(@user, :last_name))}
  = f.label :last_name, t('address.last_name'), class: 'control-label input-label'
  = f.text_field :last_name, class: 'form-control', placeholder: t('address.last_name'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :last_name)
.form-group{class: (@page_presenter.set_error_class(@user, :country))}
  = f.label :country, t('address.country'), class: 'control-label input-label'
  = f.text_field :country, class: 'form-control', placeholder: t('address.country'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :country)
.form-group{class: (@page_presenter.set_error_class(@user, :city))}
  = f.label :city, t('address.city'), class: 'control-label input-label'
  = f.text_field :city, class: 'form-control', placeholder: t('address.city'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :city)
.form-group{class: (@page_presenter.set_error_class(@user, :address))}
  = f.label :address, t('address.address'), class: 'control-label input-label'
  = f.text_field :address, class: 'form-control', placeholder: t('address.address'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :address)
.form-group{class: (@page_presenter.set_error_class(@user, :zip))}
  = f.label :zip, t('address.zip'), class: 'control-label input-label'
  = f.text_field :zip, class: 'form-control', placeholder: t('address.zip'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :zip)
.form-group.mb-60{class: (@page_presenter.set_error_class(@user, :phone))}
  = f.label :phone, t('address.phone'), class: 'control-label input-label'
  = f.number_field :phone, class: 'form-control', placeholder: t('address.phone'), required: true
  = render 'users/partials/errors_list', errors: @page_presenter.get_param_errors(@user, :phone)
