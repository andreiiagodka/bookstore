%p.lead.small.mb-10.visible-xs
  = t('user.orders.sort_by')
.dropdowns.dropdown.general-order-dropdown
  %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
    = Filtering::ORDER_FILTERING_ORDER[@filter.to_sym]
    %i.fa.fa-angle-down.dropdown-icon.pull-right
  %ul.dropdown-menu
    - Filtering::ORDER_FILTERING_ORDER.each do |filter_attribute, filter_name|
      %li
        = link_to filter_name, @page_presenter.current_url(filter: filter_attribute)
